<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>wholesalepayment</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <modules>
      <module>wholesalepayment-api</module>
      <module>wholesalepayment-dto</module>
      <module>wholesalepayment-service</module>
    </modules>

    <parent>
        <groupId>com.dcep.dips</groupId>
        <artifactId>dips</artifactId>
        <version>1.0.0</version>
        <!-- Set relativePath to an empty string to always resolve the parent POM from local or remote repositories. -->
        <relativePath/>
    </parent>

    <properties>
      <revision>1.0.0</revision>
      <gateway.version>1.2.0</gateway.version>
    </properties>

    <dependencyManagement>
      <dependencies>
          <dependency>
              <groupId>com.dcep.dips</groupId>
              <artifactId>wholesalepayment-api</artifactId>
              <version>${revision}</version>
          </dependency>
          <dependency>
              <groupId>com.dcep.dips</groupId>
              <artifactId>wholesalepayment-dto</artifactId>
              <version>${revision}</version>
          </dependency>
          <dependency>
              <groupId>com.dcep.dips</groupId>
              <artifactId>wholesalepayment-service</artifactId>
              <version>${revision}</version>
          </dependency>
          <dependency>
              <groupId>com.dcep.dips</groupId>
              <artifactId>acctrans-api</artifactId>
              <version>1.0.0</version>
          </dependency>
          <dependency>
              <groupId>com.dcep</groupId>
              <artifactId>dcclearingcenter-api</artifactId>
              <version>1.9.6.1</version>
          </dependency>
          <dependency>
              <groupId>com.dcep.infocache</groupId>
              <artifactId>infocache-api</artifactId>
              <version>2.1.8</version>
          </dependency>
          <dependency>
              <groupId>com.dcep.supergw</groupId>
              <artifactId>supergw-api</artifactId>
              <version>1.3.20</version>
          </dependency>
          <dependency>
              <groupId>com.dcep.dips</groupId>
              <artifactId>operatingcontrol-spi</artifactId>
              <version>1.0.0</version>
          </dependency>
          <!-- 货币桥网关API -->
          <dependency>
              <groupId>com.dcep.gateway</groupId>
              <artifactId>mcbdc-gateway-api</artifactId>
              <version>${gateway.version}</version>
          </dependency>

          <!-- 货币桥网关DTO -->
          <dependency>
              <groupId>com.dcep.gateway</groupId>
              <artifactId>mcbdc-gateway-dto</artifactId>
              <version>${gateway.version}</version>
          </dependency>

          <dependency>
              <groupId>com.dcep.gateway</groupId>
              <artifactId>mcbdc-gateway-mcbs-dto</artifactId>
              <version>${gateway.version}</version>
          </dependency>
          <!-- 区块链 -->
          <dependency>
              <groupId>com.dcep</groupId>
              <artifactId>bcsp-core-api</artifactId>
              <version>1.1.5</version>
          </dependency>

          <!-- hutool工具包 -->
          <dependency>
              <groupId>cn.hutool</groupId>
              <artifactId>hutool-core</artifactId>
              <version>5.8.8</version>
          </dependency>
      </dependencies>
    </dependencyManagement>
</project>
